[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gencf"
version = "0.1.1"
description = "A package to determine nonzero crystal field parameters from point group symmetries."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Li Hong Liu"}
]
keywords = ["crystal-field", "quantum"]
dependencies = [
    "numpy>=1.20.0",
    "sympy>=1.14.0",
    "tabulate>=0.9.0"
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
]

[project.urls]
Homepage = "https://github.com/Quantum-Integration-Laboratory/gencf"
Repository = "https://github.com/Quantum-Integration-Laboratory/gencf.git"
Issues = "https://github.com/Quantum-Integration-Laboratory/gencf/issues"

[tool.setuptools]
packages = ["gencf"]
